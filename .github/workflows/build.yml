name: '01 build'

on:
  push:
    paths:
        01.hw-cicd

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
            mkdir build && cd build
            cmake ..
            cmake --build .
